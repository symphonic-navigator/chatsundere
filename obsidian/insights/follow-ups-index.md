# Follow-Ups Index

A single scannable view of every deferred item across the project, so
nothing falls off the radar between sessions. **This file is an index,
not the canonical source.** Each entry links to the authoritative
discussion / brief / deferral entry where the full context lives.

Update protocol: when a new deferral is created anywhere in the repo,
add a row here. When a deferral is resolved (committed code, ADR
written, brief landed), strike the row through with `~~text~~` and
move it to the "Resolved" section at the bottom. Don't delete — the
history of what we deferred and how it resolved is itself useful.

---

## Active — Security (Larissa-tracked)

Canonical source: [[security-deferrals]]

| ID | Item | Trigger | Severity |
|---|---|---|---|
| H-1 / M-1 | Recovery wrap accepted without server-side integrity attestation | Phase 1 sync-service brief | Medium (re-classified) |
| M-3 | `changePassphraseLinkedOnline` atomicity edge case lacks crash regression test | Sync-service staging logic in Phase 1 | Low |
| M-7 | WebAuthn local-verify test does not exercise a real signed assertion | Squash D device-test scripts | Low |
| L-1 | `decodeRecoveryKey` uses non-constant-time string operations | Before v0.1.0 (if ever, low risk) | Low |
| L-B3 | Per-username login rate-limit counts successful logins | Before v0.1.0 | Low |
| L-B4 | `XDG_RUNTIME_DIR` for bootstrap CLI documentation | Before v0.1.0 in `deploy/compose.template.yml` (superseded `compose.prod.yml.example`, retired in the deployment-kit squash) | Low |
| L-A1 (2026-07-10) | `getServerSetup()` hard-fail (Finding #10b) is lazy — thrown on the first OPAQUE-touching request, not at boot or `/readyz`. A misconfigured prod instance boots "healthy" and only 500s on first passphrase auth. Larissa-confirmed Low (strictly better than the prior silent-ephemeral fallback). | Deploy/`/readyz` hardening pass before backend go-live: call `getServerSetup()` eagerly at startup or in `/readyz` | Low |
| L-A2 (2026-07-10) | Migration `0006_unique_auth_method.sql` (partial unique index on opaque auth-methods) aborts non-destructively if an existing DB already holds two opaque rows for one user (the exact pre-fix race). No prod data exists pre-v0.2.0, so N/A now. | Note for self-host upgrades: dedup opaque rows before applying 0006 | Low/Info |
| L-A3 (2026-07-10) | Unauth `/opaque/login/start` returns `wrap_aad_opaque` = `${registration-username}::opaque::v1`; a RENAMED active account is thus distinguishable + its old username readable pre-auth (Larissa whole-scope INFO-1, pre-existing). #10a closed the null-vs-present oracle but not this. | Proton-bar privacy pass before backend go-live: deliver login wrap fields at `/finish` (post-auth) like pairing already does | Low |
| L-A4 (2026-07-10) | Recovery re-pushes blob-collection records even when `blobsLanded===false` (quota/transport), momentarily leaving a ref-present/bytes-absent row; tolerated by placeholder/repair + epoch withheld so recovery re-runs (Larissa whole-scope F2). Gate the re-push on `blobsLanded` for stricter §11.5 ordering if desired. | Opportunistic, or if blob recovery is touched again | Low |
| — | Refresh-reuse user-facing notification | Phase 1 sync-service real-time channel | — |
| — | Raw MK in login-flow returns — tighten MK custody | Dedicated "Tighten crypto MK custody" squash before v0.1.0 | — |
| — | `passkey-management.ts` belongs in `packages/crypto` not user-client | Before v0.1.0 (own small squash, Larissa-audited) | — |
| L-3 (2026-05-21) | `refresh-on-401` triggers `closeAndForget` on *any* non-ok refresh response, not just reuse_detected | Coordinated with "Refresh-reuse user-facing notification" (above) — phase-1 sync-service real-time channel | Low |
| L-4 (2026-05-21) | `change-passphrase.tsx` `capturedMk` comment slightly stale post-Shape-A | Opportunistic next time the file is touched | Low |
| — (2026-07-04) | ~~Role change / transfer-primary do not deny-list still-valid access tokens; the old role claim survives until token expiry (only suspension writes the deny-list).~~ **RESOLVED 2026-07-06** (pre-test-analysis fixes): role change revokes the subject (`revokeAllForUser` + `denySub`, exactly as suspend); transfer-primary revokes BOTH actor and target. Integration tests assert the deny entries. | Low |
| F3 (2026-07-05) | ~~Multi-tab concurrent refresh trips server reuse-detection → the whole refresh-token family is revoked and both tabs hard-logout.~~ **RESOLVED 2026-07-05** (Unit A, commit `ab30e903`) — an exclusive `navigator.locks` lock (`chatsundere-token-refresh`) serialises the refresh round-trip across tabs; the module-local `refreshInFlight` guard is kept as the within-tab collapse; jsdom fallback preserved. opus whole-branch READY; not a Larissa path. | Medium (UX/availability) |
| F2 (2026-07-05) | Tier-4 step-up gate missing on most operator-privileged endpoints (suspend/unsuspend/delete-user/role-change/transfer-primary/revoke-invitation) — gated by `minRole` alone, not fresh presence. Not plain-user reachable; gate before opening admin to more than one operator. Source: 2026-07-05 auth deep-audit | Medium |

## Active — Design (Lyra-brief candidates)

Canonical sources: brief-material files in this directory.

| Item | Trigger | Origin |
|---|---|---|
| Passive auth-state visibility on profile/settings: formalise as part of a future Settings/Profile UX brief | Phase 1+ UX work | [[2026-05-20-pattern-passive-auth-state-on-profile]] |
| API endpoint shape curl-verification — five new endpoints in cross-device-identity brief | Chris exercises proposed request/response bodies with curl before Liz writes tests against them | [[../briefs/phase 0/cross-device-identity]] §Open items #3 |
| Conflict resolution on concurrent sync edits ("welcher change hat recht") | Phase 1 sync-service brief | [[2026-05-19-brief-material-cross-device-identity]] §Open Items |
| Q7 — Username-rename flow design | Phase 1 sync-service brief | [[2026-05-19-brief-material-cross-device-identity]] |
| Conditional UI (`mediation: 'conditional'`) for passkey autocomplete | Future UX-polish squash, post-Phase-0 | [[2026-05-19-brief-material-passkey-uv]] |
| Cyberpunk theming pivot — dedicated theming squash | After admin-client (Squash C) | [[2026-05-19-open-design-questions]] §3 |
| Theming mood-board curation | Chris to add 3–5 reference images before the theming squash | [[2026-05-19-open-design-questions]] §3 |
| Operator-override of TTL defaults for invitation codes | Phase 1 or later | [[2026-05-19-brief-material-cross-device-identity]] |
| Multi-account per origin in user-client | Phase 1+ (currently single-account-per-origin) | discussion 2026-05-19 |

## Active — Implementation (Liz-tracked)

Items that have been decided but not yet implemented in code.

| Item | Trigger | Notes |
|---|---|---|
| ~~Wire UV-relaxation in code (3 sites)~~ | Resolved 2026-05-21 — UV-relaxation-wiring squash (commit `e814e87`). All three ceremony sites now use UV='preferred'; PRF gate (ADR 0005) intact. Larissa-approved. | — |
| Onboarding three-path UI rewrite (QR / Manual / Local) | Brief landed (2026-05-20); awaiting API-endpoint curl-verification (Open #3) | User-client polish squash |
| Claude extended-thinking signature replay | When the agentic tool loop lands (own integrations + MCP-over-REST) — no live consumer until then; plain chat needs no replay | Build-when-needed; full rationale in [[../../superpowers/specs/2026-06-01-premium-model-integration-design]] §5.2 |
| **Lorebooks / phrase-triggered injection — reuse the Treasury `TagEditor` UX** | When the lorebook / world-info feature is designed | **Chris's explicit ask (2026-06-06):** the trigger-phrase editor must be the EXACT same UX as the artefact tag system — the shared `TagEditor` (`components/artefact/TagEditor.tsx`, chips + autocomplete, `edit`/`pick` modes). Trigger phrases ARE tags structurally. Reuse the component, don't rebuild. He loves it. |
| ~~**v0.2.0 pre-release BLOCKERS — six fixes, one commit each** (2026-07-05)~~ | **RESOLVED 2026-07-05** — integrated onto `full-backend-transition` (ff `3394f4d..9b224df2`) | **8 commits, one per fix** (the six blockers + MEDIUM-2 `compactionCheckpoints` rider + a bearerAuth deny-list close-out from Larissa's audit), each TDD + per-task review. Larissa CLEAR (commits 3–7 + re-audit of 8), Laura NO HARD DEFECTS (1–2), opus whole-branch READY TO MERGE. Gates green (typecheck 14/14, build 9/9, vitest 2714/8, auth-service baseline). Chris device-verify pending on the branch. Brief: [[../../superpowers/plans/2026-07-05-pre-release-blocker-fixes]]. |
| MEDIUM-1 — vectors never tombstoned server-side (2026-07-05) | Fast-follow after v0.2.0 blockers | `deleteDocumentCascade` deletes vectors locally only; the server keeps every `vectors` ciphertext row forever, charged against the shared 2 GiB quota and re-delivered to every fresh device's pull-from-zero. Quota banner copy ("free space by deleting documents") is thereby partly false. Monotonic leak from go-live. Source: 2026-07-05 sync deep-audit |
| MEDIUM-3 — mass-delete `findKeyByBlindId` CPU (2026-07-05) | Device probe before/around v0.2.0 | Every pulled tombstone re-derives the blind id of every `syncRows` meta in its collection (fresh `deriveDek` + HMAC import per call). 200 tombstones × a 20k-message vault ≈ millions of HKDF+HMAC ops under the sync Web Lock — looks like a hang on the mass-delete flow we ship. Candidate fix: a per-cycle blindId→key cache or a persisted reverse index. Not data loss. Source: 2026-07-05 sync deep-audit |
| ~~Built-in mindspace ids diverge per device → `settings.defaultMindspaceId` / `persona.mindspaceId` dangle cross-device, silent fallback to `mindspaces[0]` (2026-07-06, analysis #5)~~ | **RESOLVED 2026-07-07** (this branch) | Deterministic slug ids (`mindspace-builtin-<name>`) via the provider-fix pattern: Dexie v36 rekeys the seeded seven and remaps `settings.defaultMindspaceId`, `personas.mindspaceId`, `chats.resolvedMindspaceId` (a third synced reference the finding missed) and trash snapshots in one transaction. Built-ins stay sync-excluded; convergence follows from identical seeding. Spec: [[../../superpowers/specs/2026-07-07-builtin-mindspace-deterministic-ids-design]]. Source: [[../../PRE-TEST-ANALYSIS-v0.2.0]] Q4 |
| ~~Inkling `reasoningTraceHidden` → drop when nano-gpt wires the passthrough (2026-07-16)~~ | **RESOLVED 2026-07-17** — nano-gpt wired the passthrough (Milan's "will fix asap") | Trace now surfaces on both slugs; `reasoningTraceHidden` dropped from the offering. The fix also exposed a real `effort` ladder, so Inkling's reasoning was re-modelled from `toggle` to a four-band `steps` control (only four of the card's seven levels separate under measurement). `run-inkling-suite.ts` reverted to the standard matrix (`permutationsForReasoning`), core 44/44. The `reasoningTraceHidden` MECHANISM is retained but now has no consumer. Source: [[../models/inkling]] |
| Inkling freedom judgement pending Lex's safetymaxxed bench (2026-07-16) | **OpenRouter reached 2026-07-18** (`c2112bbd`) — now waiting only on Lex running his bench | Revisit `freedomOriented` (currently `null` → the `Uncensored?` badge) with his results rather than guess. The first trigger condition is met; ping Lex that Inkling is live on OpenRouter (Together). Source: [[../models/inkling]] |
| **Nemotron 3 Ultra deferred — no self-invoked tools** (2026-07-18) | If the model gains tool-eagerness under `tool_choice:auto`, or if we ever adopt a tools-off curation lane | Probed live on nano-gpt (`nvidia/nemotron-3-ultra-550b-a55b`): the tool MECHANISM works (fires cleanly under `tool_choice:"required"` → 2 deltas) but the model **never self-invokes** under `auto` — 0 fires even when the prompt explicitly says "use the generate_image tool". The app **always** sends `auto` (never `tool_choice`), so `generate_image` and any agentic feature are dead for it. Conversation-suite went red 4× on `tool-call-fired`. Deferred rather than shipped tools-off (every curated model currently supports tools; a tools-off lane would need the suite reworked). Freedom also unverified (`null`, community-hearsay only — mirrors Inkling). Chris's call 2026-07-18. |
| Uplift interruption at `join/finish` strands the invitation (`code_already_redeemed`) (2026-07-06, analysis #7) | Runbook entry now; structural fix (redemption idempotency ticket or finish-retry token) is a design question for Lyra/Chris | The code is burned server-side before the local `linked_account` row is written; a lost response forces a fresh invitation. Admin remedy works (mint a new invitation) — document it for operators. Source: [[../../PRE-TEST-ANALYSIS-v0.2.0]] Q6 |
| ~~Generic sync transport failures are invisible (`void cycle()` swallows; only typed per-record errors raise attention) (2026-07-06, analysis #8)~~ | **RESOLVED 2026-07-07** (branch `claude/pre-test-analysis-open-items-6s118y`) | Three consecutive failed cycles (counted only while connectivity reads `linked_online`, so airplane mode never false-alarms) raise a new self-healing `transport_failing` attention; the next completed cycle retires it, resets the streak, and stamps the previously never-written `lastSyncAt`. Renders app-wide via the existing `GlobalSyncLine`; collapsible to the dot there (Laura soft — no affordance to hide). Laura pre-squash: no hard defects. Source: [[../../PRE-TEST-ANALYSIS-v0.2.0]] Q4 |
| ~~Pairing F4/F5 — unmapped join error codes (2026-07-05)~~ | **RESOLVED 2026-07-05** (Unit B, commit `a918e3b4`) | Five lifecycle codes (`code_expired`, `code_already_redeemed`, `code_attempts_exhausted`, `rate_limited`, `session_expired`) now map to constructive, flow-tailored messages in both onboarding confirm handlers; `shared-types` `JoinError` reconciled with the server (phantom `rate_limit_exceeded` removed, four real codes added); server `code_already_redeemed` unified to **410 Gone** (+ a new integration test on the previously-untested guard). Larissa CLEAR TO SQUASH; opus whole-branch READY. Spec/plan: [[../../superpowers/specs/2026-07-05-onboarding-auth-hardening-design]]. |
| ~~`recovery.tsx` phantom `rate_limit_exceeded` literal — finish the phantom-code cleanup (2026-07-05)~~ | **RESOLVED 2026-07-13** (Unit D, squash `cf5b3f77`) | The pre-go-live fix bundle's onboarding-recovery fix (Unit D1) dropped the phantom `rate_limit_exceeded` literal and now branches on the real server code `rate_limited` alongside the `429` status check, with honest `Retry-After`-derived copy. Original: `routes/onboarding/recovery.tsx` tested `err.code === 'rate_limit_exceeded'`, the dead string Unit B removed from `JoinError`. |
| Memory-page `no-credentials` error says "re-authenticate, then retry" but offers only a Retry button — no visible route to step-up/re-auth (2026-07-14, Laura soft on the memory-robustness spec-pass; pre-existing) | Opportunistic, next time the memory page or the step-up surface is touched | Confirm the global step-up prompt is reachable from this state, or make Retry re-resolve credentials and trigger step-up itself. Source: [[../../superpowers/specs/2026-07-14-memory-consolidation-robustness-design]] |
| Refresh fetch has no `AbortController`/timeout — under the new cross-tab lock a stalled refresh holds the lock for other tabs (2026-07-05) | Opportunistic / a robustness pass | `classifyRefresh` (`lib/fetch.ts`) issues the refresh fetch with no timeout; now that it runs under the F3 `navigator.locks` lock, a pathologically stalled socket makes other tabs' refresh *await* until the browser-level network timeout. Bounded (browser timeout + lock auto-release on tab close), so **not** a wedge/deadlock — Larissa and the opus review both confirmed it is strictly better than the concurrent-reuse status quo. Candidate: thread an `AbortController` with a timeout into the refresh fetch. Pre-existing; surfaced by the F3 whole-branch review. |
| ~~Pairing F7 — QR base-URL convention split (2026-07-05)~~ | **RESOLVED 2026-07-13** (Unit A, squash `84d3f7dd`) — **one live scan of both QR forms against the staged prod topology still owed to Chris (go-live checklist)** | Fixed by the pre-go-live fix bundle: all three mint sites route through one shared `buildJoinQrUrl` helper (auth-service) that strips `/auth` uniformly (closes the blocker) and, when the new optional `APP_PUBLIC_URL` env is set, points QR codes at the **client origin** `${APP_PUBLIC_URL}/join?server=…#code`; the client gains a public `/join` chooser route so a system-camera scan lands on a real screen. `parseJoinUrl` accepts both the new client-origin form and the legacy form. Larissa CLEAR, Laura clean. Spec/plan: [[../../superpowers/specs/2026-07-13-pre-golive-fix-bundle-design]]. Source: 2026-07-05 pairing deep-audit + 2026-07-13 release-day audit |
| AI Providers detail page: migrate Save/Remove from raw `<button>`/inline styles to the `Button` primitive | Next time the provider page is touched (makeover-wide "single uniform flows" unification) | Pre-existing inconsistency surfaced by the opus whole-branch review at the My Integrations squash (`c10f4785`, 2026-06-25). The new Integrations detail page uses `Button`/`ConfirmDialog`; provider still has raw chrome. Not a regression — advisory unification. |
| ~~Self-host the embedding-model weights — drop the HuggingFace-CDN dependency~~ | **CORRECTED 2026-06-07 — premise was false.** The embeddings engine is **already self-hosted** (`env.allowRemoteModels=false`, `localModelPath='/model/'`) — the runtime never calls HF. There is no runtime CDN dependency to drop. Superseded by the two real items below. See [[security-deferrals]] 2026-06-07 (corrected). |
| Embedding weights: production deployment must serve `/model/` | Before the encrypted-backend / hosted-instance work (Block 6) and any prod build of the user-client | **2026-06-07 (Chunk A device-test finding).** The ~310 MB int8 weights must be provisioned at the user-client's `/model/` for embedding to work — a build/deploy step, not runtime. Dev: `pnpm --filter @chatsundere/user-client fetch-model` (gitignored under `apps/user-client/public/model/`); the Vite build copies `public/model` → `dist/model`. The instance/Docker image must include or serve these. Document in `DEPLOYMENT.md`. |
| Embedding weights: pin SHA256s in `fetch-model.mjs` | Opportunistic / before v0.2.0 | **2026-06-07.** `packages/embeddings/scripts/fetch-model.mjs` has an empty `EXPECTED_SHA256` map (scaffolded). Pin the printed hashes (and ideally the HF `REVISION` to a commit SHA) so a tampered/changed upstream weight is caught at fetch time. |
| **Self-host the ORT WASM runtime — drop the jsdelivr runtime dependency** | Before v0.3.0 (encrypted backend / public instance); ideally with the prod `/model/` deploy work | **2026-06-07 (Chunk A device test, real Zero-Knowledge gap).** transformers v4.2.0 defaults `env.backends.onnx.wasm.wasmPaths` to `https://cdn.jsdelivr.net/npm/onnxruntime-web@<ver>/dist/ort-wasm-simd-threaded.asyncify.{mjs,wasm}` — so while the **model** is self-hosted (`/model/`, never HF at runtime), the **ONNX runtime WASM** is fetched from jsdelivr at runtime (IP + version disclosure to a CDN; no user content). Fix: fetch those ORT files at setup (like the model), serve them same-origin (e.g. `/ort/`), and set `env.backends.onnx.wasm.wasmPaths` to that path in `packages/embeddings/src/engine/execution.ts`. See [[security-deferrals]] 2026-06-07 (ORT runtime). |
| **Stufe B — production cross-origin isolation (COOP/COEP) for multi-threaded WASM embeddings** | At the alpha-deploy milestone (Block 6 / public instance). **Parked 2026-06-18** — low ROI now. | **2026-06-18 (embedding-speed work).** Stufe A landed dev/preview isolation (`a1fdd8a`: COOP `same-origin` + COEP `credentialless` in `vite.config.ts` → `crossOriginIsolated` → 4-thread WASM, ~3.6× on non-WebGPU setups). Prod is **GitHub Pages → cannot set headers**, so prod isolation needs **SW-based COI injection** via the existing VitePWA service worker. For **Safari** COEP must be **`require-corp`** (credentialless is Chrome/FF-only), which breaks cross-origin no-cors subresources → requires a cross-origin audit + fixes: self-host the ORT WASM (**couples with the "Self-host the ORT WASM runtime" row above**), the VAD/`vad-web` jsdelivr loads (`lib/voice/dictation/capture.ts`), Google Fonts, and verifying provider fetches / CORS-proxy / web-fetch. **Why parked:** only benefits deployed users **without** real WebGPU (GPU users already get fast q4f16); alpha isn't deployed. Embedding ORT WASM is already same-origin (`import.meta.url`), so dev credentialless was safe. |
| Knowledgebase: NSFW deep-link gating of the library-detail route (`/app/knowledge/:libraryId`) | When deep-link / out-of-mode gating is formalised app-wide | **Deferred 2026-06-07 (Chunk A holistic review, Minor).** The detail route reads the unfiltered `useLibraries()`, so a direct link to an NSFW library (or switching to SFW while on the page) shows it. Consistent with the persona-editor precedent (deep-links to NSFW personas aren't gated either); the security-relevant retrieval-time NSFW filter is Chunk B. Decide app-wide: accept the precedent or redirect-to-list on out-of-mode open. |
| Knowledgebase: remove the deprecated `NewLibrarySheet` alias | Opportunistic next time the file is touched | **Minor cruft (Chunk A).** `NewLibrarySheet.tsx` was generalised to `LibrarySheet`; a deprecated `NewLibrarySheet` alias export remains but is unused. Drop it. |
| Attach-document: invalidate the document-reference preview on source edit | Opportunistic / when knowledge editing is next touched | **Deferred 2026-06-07 (Chunk B2 opus holistic review, Minor — preview only).** `usePendingDocumentContents`' query key `['attachments','ref-contents',sig]` (`data/attachments.ts`) is keyed only by attachment+document id and is never invalidated on `updateDocument`. Editing a library document while a copy-on-write reference is still pending leaves the cockpit/lightbox **preview** showing the old content. The **sent** content is always correct (`snapshotPendingDocumentReferences` reads fresh `doc.content` at send). Fix: invalidate `['attachments','ref-contents']` from `useUpdateDocument`'s `onSuccess`, or key the query by `updatedAt`. |
| Attach-document / knowledge: wrap `deleteLibraryCascade` in a single transaction | Opportunistic / if cross-document atomicity ever matters | **Deferred 2026-06-07 (Chunk B2 reviews, Minor).** `deleteLibraryCascade` (`data/knowledge.ts`) issues a sequence of independent transactions (per-doc materialise + vector delete, then bulk row delete, then binding pruning). Each step is idempotent and materialise-before-delete ordering holds, so a mid-cascade crash leaves at worst an early-frozen pending reference (harmless). Acceptable for a local-first store; wrap the loop in one `rw` transaction if atomicity is ever required. |
| Cockpit: add `aria-haspopup="menu"` to the `(+)` and `(⋯)` menu trigger buttons | Opportunistic accessibility pass | **Deferred 2026-06-07 (Chunk B2 review, Minor — pre-existing, app-wide).** Both cockpit menu-trigger buttons open a `role="menu"` but omit `aria-haspopup="menu"`, so screen readers announce them as toggles rather than menu buttons. Pre-existing across the file (not introduced by B2). One attribute each. |
| **WATCH: lightbox "Remove" intermittently leaves the counter stuck (e.g. `4/4`) on a mixed pending set** | If it recurs — run the probe first, then capture | **Observed 2026-06-08 (Chris device test), NOT yet reproducible.** Clues: the failing set **mixed** an image upload + an HTML artefact-snapshot + a KB document-reference, and he had **just tried to delete a KB document** in parallel. Could not reproduce a second time. Logic fully exonerated in jsdom: remove of last/middle/all items, mixed origins, real async + StrictMode all pass. Leading suspect = a UI-cache-coherence gap: `deleteDocumentCascade`/`deleteLibraryCascade` (`data/knowledge.ts`) write to the `attachments` table via `materialiseReferencesForDocument` but never invalidate `QK.attachmentsPending` — opening a stale-strip window (explains "DB and UI drift", though materialise changes `text`, not membership, so not the whole story). Secondary: image object-URL revoke churn (`Cockpit.tsx:128`). **On recurrence:** run the IndexedDB pending-vs-DOM probe (in the 2026-06-08 chat) BEFORE reloading — if DB-pending < counter → invalidate the pending query from the KB-delete path; if DB-pending == counter → the delete never fired (touch-miss / mutation). |
| **Code-split the user-client main chunk (shiki core remaining)** | When headroom tightens again; opportunistic with a perf pass | **2026-06-11 (TEAL squash), partly paid 2026-07-18.** The main chunk sat **<1 kB under** workbox's 2 MiB precache cap — any feature would have tripped it (TEAL did). Stopgap raised to `maximumFileSizeToCacheInBytes: 3 MiB`. On 2026-07-18 the July-curation commit (`efe119bf`) + third-party-import jointly pushed it **past** the 3 MiB cap (build red); **fixed by splitting katex into its own chunk** (`manualChunks`, commit `6714b14c`) → index chunk **2.88 MB**, ~188 kB headroom. mermaid is already dynamically imported; **shiki core remains** eagerly bundled (its language grammars are already lazy) and is the next win if the chunk grows back toward the cap. |
| TEAL streaming: sung span split across chunks shows doubled ♪ mid-stream | Accepted transient; revisit only if device tests find it jarring | **2026-06-11 (TEAL opus holistic review, Minor).** `.teal-singing::before/::after` attach ♪ per span; a `<singing>` wrap crossing a chunk boundary briefly renders `♪ one ♪♪ two ♪`. Heals on finalise (rehype emits one span per text segment). |
| TEAL streaming: stale `atLineStart` after a cross-boundary tag carry | Accepted transient (documented `TODO(teal)` in `teal-streaming.ts`) | **2026-06-11 (TEAL reviews, Minor).** A fence opening at the exact first byte after a carried tag fragment is missed by the streaming view; the finalised Markdown pass renders correctly. |
| Voice: read-control discoverability behind tap-to-expand | The design-language pass | **2026-06-11 (Laura pre-squash, advisory — not a finding).** Entry to voice costs a tap-to-expand to find the Read control while governance (transport) is persistent — intentional asymmetry per spec §4, but the initial Read affordance deserves a deliberate look when the design language lands. |
| Voice: VoicePicker sibling retry does not heal the other picker | Opportunistic / if roleplay users report it | **2026-06-11 (Task-9 review, Minor).** Two open pickers share one module-level voices fetch; after a shared failure, Retry on picker A refetches but picker B's local error state stays until its own Retry. Workaround is one tap. |
| Voice: sentence-glow degrades to paragraph glow for multiline-display-math paragraphs | Accepted degrade; revisit only if device tests find it jarring | **2026-06-11 (holistic review I1 fix).** A raw paragraph split by `preprocessMath` (matrices, aligned) is glow-anchored as a whole paragraph, never sentence spans — by design of the raw↔processed mapping; never mis-highlights. |
| Voice: per-message speakability memo re-segments all persona messages on change | Profile on very long chats; opportunistic perf pass | **2026-06-11 (Task-7 quality review, Minor).** `ChatStream` memoises on `[messages, voiceMode, roleplay]`; any message change re-runs `segmentMessage` over every persona message. Pure string work, bounded; per-message memo (id+updatedAt) if jank shows. |
| Voice: transport vertical offsets are estimated tuning knobs | Chris's styling pass | **2026-06-11.** Interaction-mode lift (5.5rem) and reading-mode affordance clearance (3.5rem) are commented estimates in `index.css`, not measured values. |
| Voice: VoicePicker has no outside-click close | Opportunistic polish | **2026-06-11 (Task-9 self-review).** Consistent with other custom dropdowns (AddProviderPicker); add app-wide when those get it. |
| ~~`/api/admin/invitations` endpoint~~ | Resolved 2026-05-22 — Squash α (commit `9b170c1`); reshape returns `code` + `qr_url` and accepts `suggested_username` + `note`; Tier 4 step-up gate wired. | — |
| ~~`/api/me/pairing-codes` endpoints~~ | Resolved 2026-05-22 — Squash β (commit `7a01697`); POST Tier 1 gated, GET surfaces code/qr_url as null (HMAC-only storage, spec §4.5 deviation tracked below), DELETE with 404 foreign / 409 already-revoked. | — |
| ~~`/api/join` endpoint with atomic code validation~~ | Resolved 2026-05-22 — Squash β (commit `7a01697`); unified two-round flow per ADR 0028, kind discriminator (invitation \| pairing), atomic UPDATE-WHERE redemption, `assertOpaqueWrappingPresent` defence-in-depth on pairing finish. | — |
| ~~`pending_codes` DB table (single table with `type` discriminator)~~ | Resolved 2026-05-22 — Squash α migration 0003 + 0004 (role nullable). Plus migration 0005 (commit `cffeb0b`, Squash γ) added `opaque_client_identifier` so OPAQUE login + step-up survive username changes. | — |
| Pairing-code GET surfaces `code: null` and `qr_url: null` (spec §4.5 deviation) | Surfaced 2026-05-22 in Squash β — HMAC-only storage means the plaintext is unrecoverable after the POST response. Revisit at v0.1.0+ if user feedback wants post-creation re-display. | Either store plaintext encrypted under a server key, or freeze the null behaviour as final. |
| ~~`TRUSTED_PROXY_IPS` env var + X-Forwarded-For trust model~~ | **Resolved 2026-07-10** — landed as `TRUST_PROXY_HOPS` (hop-count model, not an IP allow-list), porting the audited proxy/sync `deriveClientIp` into auth: `ipKey()` now reads the trusted hop from the RIGHT of X-Forwarded-For over the injected socket peer, so per-IP limits are no longer client-spoofable. Larissa CLEAR. See [[security-deferrals]] L-β-2. | — |
| Switch from native UUIDv4 to UUIDv7 (`uuidv7` npm package on client, `gen_uuidv7()` SQL function on server) | Before any new entity DB tables land; documented in ADR 0025 | Cross-cutting; library-based |
| Auto-handover client state machine with failure-mode handling | After cross-device-identity brief + ADR 0026 land (both done 2026-05-20) | User-client; per pattern in [[2026-05-20-pattern-frontend-changes-affecting-crypto-semantics]] consider Larissa-pass on the state-machine file specifically |
| Partial-upload cleanup endpoint `DELETE /api/me/account` on handover-cancel | Co-requisite of auto-handover client state machine | Per ADR 0026 §Failure Mode C; Larissa-audit |
| ~~`POST /v1/auth/step-up` endpoint + step-up middleware on all Tier 1+ endpoints~~ | Resolved 2026-05-22 — Squash γ (commit `cffeb0b`); unified `/api/v1/auth/step-up/{start,finish}` mechanism-discriminated (webauthn \| opaque), requireStepUp helper covers Tiers 1/2/3/4, logout cascade clears `step_up:<jti>:*`. | — |
| `<StepUpModal />` component + request interceptor in user-client | After step-up brief + ADR 0027 land | User-client; centralised interceptor catches `403 step_up_required` and surfaces modal transparently |
| Tier-4 step-up integration in admin-client | After admin-client invitation-creation UI exists (Squash C) | Reuses `<StepUpModal />` with 5-minute grace window |
| HTTPS-required + server-at-root + `/api` prefix enforcement in user-client | Per ADR 0023 | Likely already true; verify and document |
| Theming squash | After Squash C (admin-client) | See design-deferrals |
| Remove alpha CORS-proxy scaffolding (`CorsProxyBlock`, `SettingsRow.corsProxy`, `✗ Needs proxy` status, `requires-proxy` client routing, the proxy-missing save guard) | Beta proxy-service (Phase 2) lands — proxied providers then reach upstream over the user's server connection | [[../../superpowers/specs/2026-05-31-provider-model-handling-rework-design]] §13 |
| `MessageBlock` not memoised — `renderBlocks()`/`groupAdjacent()`/`join()` re-run per streaming token for every historical message (the expensive remark/rehype/shiki pipeline IS skipped via `MarkdownContent`'s memo) | When long-chat streaming jank is observed, or alongside the next chat-perf pass | Surfaced by the opus final review of the message-rendering squash (`433cd79`). Fix: wrap `MessageBlock` in `memo` with a custom comparator — requires `useCallback`-ing ChatStream's inline `onToggleExpand`/`onCopy`/`onBookmark`/`onRegenerate` and deriving pill-map equality (the very prop-complexity the plan avoided by memoising `MarkdownContent` instead). Correctness is fine today; this is a perf refinement. |
| Message-rendering main-chunk weight — `katex` + `react-markdown` load synchronously in the entry bundle (~598 KB gzip) | If the v0.1.0 PWA initial-load budget tightens | `shiki`/`mermaid` already lazy-split. Could lazy-load `MarkdownContent` or split `katex` out. Matches chatsune (katex is synchronous there too). Surfaced 2026-06-01 during the message-rendering build. |
| Route consented model-image loads through `proxy-service` so even a deliberate load doesn't expose the user's IP | Beta proxy-service (Phase 2) lands | Today `ImageMarker` (commit `fd30cb5`) gates remote Markdown images behind a tap-to-load pill (`referrer-policy: no-referrer`, no persistence) — the privacy leak is closed for auto-load, but a consented load still beacons the user's IP/timing to the third party. Once the proxy exists, fetch through it. Surfaced 2026-06-02. |
| **JSX / React single-page-app preview in the lightbox (Chris's explicit ask — core functionality for many users)** | When the artefact-generation work lands — JSX/SPA preview belongs to it, not to the viewer | Deferred from the lightbox-viewer work (2026-06-06). For many users, building a concept quickly, demoing it, and trying it live is "gold". MUST use a **locally-bundled** transpiler (sucrase or esbuild-wasm, lazy-loaded) + React from our own deps, inlined into a hard-sandboxed iframe (`allow-scripts`, NO `allow-same-origin`) — **never** a third-party CDN. The chatsune `unpkg.com` approach (loading React + Babel at runtime) was rejected on zero-knowledge grounds (IP-leak + remote-code surface). The lightbox already has the format-dispatch seam (`format-detect.ts` + `previews/`) to plug a `jsx` viewer into. Spec: [[../../superpowers/specs/2026-06-06-lightbox-viewer-design]] §11. |

| Widen the lightbox's `detectFormat` `LANG_BY_EXT` (and the shiki loaded-langs set) so more saved code-block languages syntax-highlight in the preview | Opportunistic, or when a user notices a saved snippet rendering plain | Surfaced by the opus holistic review of save-as-artefact (Chunk 4, commit `7c907e5`). In chat, *any* fence language gets a Save button, but `detectFormat` (`components/lightbox/format-detect.ts`) only recognises ~18 extensions — saving e.g. `kotlin`/`swift`/`dart`/`elixir`/`dockerfile`/`graphql` yields a correct `format:'code'` artefact (right Treasury tab, downloadable) that renders **un-highlighted** in the lightbox. Pre-existing `detectFormat` behaviour (same for uploads), only made wider by Chunk 4. Fix: extend `LANG_BY_EXT` + ensure the langs are loaded in `lib/markdown/highlighter.ts`. Content + category are always correct; only highlighting degrades. Non-blocking. |
| Read-only home logo: splash-FLIP on a cold-start *directly into* a reading-mode chat URL animates the large centred splash logo into the tiny `brand-logo-small` slot | Opportunistic / if the odd FLIP is ever noticed | **Deferred 2026-06-08 (chat-polish opus holistic review, Minor, confidence 30).** Previously the reading-mode logo was absent so `topbarLogoRef` was null and the FLIP bailed; now the ref is assigned, so on a rare cold-start deep-link into a reading-mode chat the FLIP targets the small logo (visually odd, but safe — the rect is measured live, no crash/wedge). Only matters if a route changes mid-FLIP. Fix if ever seen: skip the FLIP when `isReadingChat` at splash-dismiss time (`SplashOverlay.tsx`). |
| Cancel inference: a Stop fired *during* the substitute-vision describe phase doesn't cancel the in-flight one-shot describe call | Opportunistic / alongside the describe-signal plumbing | **Deferred 2026-06-08 (chat-polish opus holistic review, Minor, confidence 40).** `abortPreserve` aborts the stream controller, but `runOneShotCompletion` for the substitute-vision describe ignores the signal (`_signal` is documented reserved/unused in `resolveUserContent`). The send tears down cleanly (the `.catch` early-returns on the deleted handle — no double-finalise/wedge), but one describe request may complete uselessly. Pre-existing signal-plumbing limitation, only reachable now that user-Stop exists. Fix: thread the abort signal into the describe one-shot. |
| TTI: object URLs from `artefactToViewable` image items are never revoked (Treasury / chat-page ad-hoc mapping) | Opportunistic / if long-session memory pressure is ever observed | **Deferred 2026-06-09 (TTI Task-11, Minor).** `artefactToViewable` mints an object URL per image artefact; its callers (`treasury.tsx`, `chat-page.tsx`) map rows ad hoc with no revocation hook, so every Treasury filter change mints fresh URLs that live until page unload. Matches the pre-existing creator-revokes attachment contract (these creators simply don't); documented in the function's JSDoc. Bounded but real for a long-lived PWA session. Fix: caller-side revocation effect. ImagePill's own URLs ARE revoked (cleanup effect, staleTime Infinity guards the lightbox). |
| Abort mid-tool-run discards in-flight tool-call pill rows (pre-existing, ALL tool pills — surfaced by TTI) | Opportunistic / alongside the next abort-path work | **Noted 2026-06-09 (TTI final opus holistic review, Minor, pre-existing).** `abortPreserve` and the stream `.catch` persist only `lorePill` + `visionPills`, never `result.pillRows` — Stop after a tool pill block was seeded leaves the block without its row on reload (renderer degrades to null, no crash; generated image artefacts themselves survive in the Treasury). Identical for expert/MCP/knowledge pills today; the TTI review merely surfaced it. Fix: persist tool pillRows on the abort/catch paths too. |
| TTI: NSFW-slot enabled branch has no test until the first `canDoNsfw` model is curated | When the first NSFW-capable image model is curated | **Noted 2026-06-09 (TTI Task-9).** The NSFW select's enabled state (a `canDoNsfw` offering existing) is implemented but unreachable with the real catalogue, so only its two disabled-copy states are test-pinned. When curating the first NSFW model: add the enabled-branch test + the `nsfw: true` end-to-end device test (spec §13.7 inverts). |
| ~~Roleplay: context-token gauge omits the roleplay segment (~300-400 tokens) for roleplay personas~~ | Resolved 2026-06-11 same-day (Chris's ask) — the gauge now passes `roleplayEnabled`/`narration`/`personaName` AND filters openers out of `msgTexts` via `isContextMessage` (same honesty class). The gauge's lore/knowledge omissions remain by design (per-send data, unknown at render time). | — |
| ~~Persona editor: Behaviour accordion density (seven controls in one card)~~ | **Resolved 2026-06-27 — the My Circle + persona makeover dissolved the accordion entirely.** The seven controls are now split across the Instructions / Roleplay / Model-behaviour sub-pages of the Persona Hub (one intent per screen). | — |
| Surface a global "Reduce animations" toggle bound to the existing `SettingsRow.animationsEnabled` field | Design-language / a settings/appearance pass | **Laura SOFT-2, spectrum-analyser pre-squash 2026-06-14.** The spectrum analyser (and the breathing orbs) already rest when `settings.animationsEnabled` is false, but that field has **no user-facing UI anywhere** in the client — so the spectrum spec's §8 framing of "global animations-off" as an escape hatch is currently aspirational. The analyser's real escapes reduce to its own per-feature toggle + OS reduced-motion (which fully cover escaping the feature, hence non-blocking). Fix: add a "Reduce animations" toggle (likely Settings → general/appearance) bound to `animationsEnabled`; several components already honour it. |
| Persona makeover: thread `?return=` through the Hub's NavTiles + each sub-page `back` | Opportunistic / v0.1.0 polish | **Flagged 2026-06-27 by both the opus whole-branch review AND Laura's pre-squash (SOFT).** The Hub reads `?return=` (e.g. a chat that opened it) and binds its back/crumb to it, but the eight `NavTile` links and every sub-page's `back` target the bare `/app/persona/:id` — so chat → hub(`?return`) → sub-page → back lands on the hub *without* the return param, and the next back goes to `/app/circle` instead of the originating chat. **Not a dead-end** (the hub's gold Continue resumes that same chat, one tap), hence shipped as-is. Fix: thread `return` through `hub.tsx` tile `to`s + the sub-page back props (or stash it). |
| Persona makeover: extract a shared `useApplyChatsuneImport(personaId)` helper | Opportunistic next time the import paths are touched | **opus whole-branch review 2026-06-27 (Minor).** The Chatsune session/memory import-toast logic (pluralisation + `invalidateQueries`) is duplicated byte-for-byte between `create.tsx` (staged-on-create) and `hub.tsx` (write-now merge). Low drift risk today, but the only place the British-English import copy could silently diverge. ~30 lines removable. |
| Persona makeover: `create.tsx` draft snapshots possibly-unloaded query data | Opportunistic / if a cold deep-link to `/app/persona/new` is reported | **opus whole-branch review 2026-06-27 (Minor).** `useState(() => defaultDraft(settings, mindspaces, providers))` runs once at mount; a cold deep-link straight to the create page (queries unresolved) keeps the gold fallback colour + empty providerId and never re-syncs. My Circle (the normal entry) warms all three queries first, so this only bites a direct deep-link; and there is no colour control on the create page, so the persona simply starts with the gold accent until the user visits Mindspace. Fix: recompute colour/providerId from live data at `onCreate`, or guarded re-sync while the draft is untouched. (Also trivial: the create "remove avatar" path fires a no-op `removeAvatarMut` for a row that has no avatar.) |
| Global "sync paused" attention surface (quota / tamper / recovery-paused) | Design-language pass / first alpha signal | **Laura holistic note (SOFT-3), Full Backend Transition pre-squash 2026-07-03.** The sync attention states render only on `SyncStatusLine`, which lives only on `/app/account/server-linking` — a user who never opens that sub-page never learns sync has stopped. **Spec-sanctioned** (WS-C decision 2 deliberately scoped "one minimal status line on the account page"), so not a regression and non-blocking. `SyncSurfaceHost` (`root.tsx`) already proves the global-hook pattern; a global surface for the "sync paused" *class only* (tamper + recovery-paused, not the routine states) would fit it. Sibling: SOFT-1 (in-chat connectivity cue) was **fixed** in `8d901f45`. |
| ~~Onboarding: while `linkStatus === 'linked'` and the first post-link sync has not completed, suppress the SetupCard + show "Syncing your account…"~~ | **RESOLVED 2026-07-07** (branch `claude/pre-test-analysis-open-items-6s118y`, analysis #9) | New `useFirstSyncPending` (linked && `linked_online` && `lastSyncAt === null` — the latter now stamped by every completed cycle, so the gate terminates even on a genuinely empty account) swaps the SetupCard for a calm, non-gold, non-interactive `FirstSyncCard` ("Syncing your account…"). Local-only and offline/unreachable devices keep the SetupCard (the cue could never resolve there). Laura pre-squash: no hard defects; 3 residual softs ruled acceptable-as-built (one-tick cue→card flash for returning users, tile metas still read "empty" during the pull, cue persists on an empty account behind a failing server — pairs coherently with the analysis-#8 banner). Original: **Laura pre-squash SOFT, 2026-07-05** — the SetupCard's "Create your first companion" during backfill brushed active-misdirection (duplicate-persona risk). |
| ~~Embeddings `VectorStore.update()` bypasses the configured byte budget~~ | **RESOLVED 2026-07-06** (commit `01e0af80`) — `update()` now runs the same `enforceBudget` delta check as `upsert` (honours `onFull`/`BudgetExceededError`; shrinking updates always allowed). TDD, discriminating tests (fail on old code). Not a Larissa path. | **Codex external review 2026-07-06 (their #4, Anicka's tester).** `update()` (`packages/embeddings/src/store/vector-store.ts:155-165`) recomputes `bytes` but — as its own JSDoc states — skips `enforceBudget`, so repeated `metadata`/`numeric` updates on existing rows can grow stored data past `maxBytes` while `upsert` enforces it. Client-local IndexedDB soft-budget only (no attacker but the user's own data; adds no rows), so **Low** not Medium. Fix: a delta-byte check against `maxBytes` inside `update`, honouring `onFull`/`BudgetExceededError` like `upsert`. Bundled-review sibling of the `rowBytes` throw below. |
| ~~Embeddings `rowBytes()` throws on structured-clone-valid-but-non-JSON metadata (BigInt, circular)~~ | **RESOLVED 2026-07-06** (commit `01e0af80`) — metadata sizing now falls back to a fixed conservative estimate when `JSON.stringify` throws or returns `undefined` (BigInt / circular / function). New `schema.test.ts` covers both. | **Codex external review 2026-07-06 (their #6).** `rowBytes` (`packages/embeddings/src/store/schema.ts:40-42`) `JSON.stringify`s `tags`/`numeric`/`metadata`; IndexedDB structured-clone accepts values (`BigInt`, circular graphs) that `JSON.stringify` throws on → an uncontrolled throw on the write path (`toRow`, line 86) instead of a controlled validation error. Trivial-Low: `metadata` is `unknown` but we populate it ourselves; nobody puts a BigInt in today. Fix: `try/catch` around the stringify with a fallback size estimate. |
| ~~ChatGPT `chatgpt-4o-2024-11-20` on OpenRouter 404s under our data policy~~ | **RESOLVED 2026-07-06** — Chris opened the OpenRouter account data policy (<https://openrouter.ai/settings/privacy>) to allow the OpenAI-direct endpoint (the checkpoint's only endpoint; base `gpt-4o` falls back to Azure). Suite then green → offering lifted to `confidence: 'verified'`. Caveat retained in the Record: users with a locked-down OR policy still 404 → nano-gpt is the reliable route. Record: [[../models/chatgpt]]. | — |
| ChatGPT GPT-5 family reasoning summary is stochastic on OpenRouter | If we later find a reliable way to force OpenAI's summary via OpenRouter, lift to `verified` | **Curation caveat 2026-07-06 (OpenAI onboarding).** With `include_reasoning: true` the reasoning summary surfaces only intermittently on OpenRouter (reproduced on gpt-5.1 + gpt-5.4: same effort, text present in one run, absent in another) — reasoning genuinely happens (tokens/tools/vision/usage all green), only the visible chain-of-thought is unreliable. Reliable on nano-gpt (native `delta.reasoning`). OR GPT-5 offerings kept at Chris's call with `confidence: 'partial'`. Record: [[../models/chatgpt]]. |
| ~~Client-data resurrection: prior identity's rows survive a new-identity/MK change and resurface on login (`OperationError` at `openSecret`)~~ | **RESOLVED 2026-07-06** — client-data is now bound to the MasterKey identity: a non-secret one-way tag (`deriveIdentityTag`) is stamped on the settings row; a boot guard (`enforceClientDataIdentity`, run before `setSession` on every unlock) wipes client-data + knowledge-vectors on a tag mismatch, and onboarding wipes-before-persist gated on a fresh device (`wipeClientDataForFreshOnboarding`). Larissa CLEAR (3 rounds; caught + closed a HIGH the fix introduced). Spec: [[../../superpowers/specs/2026-07-06-client-data-identity-isolation]]. Discovered live 2026-07-06 (two nano-gpt provider rows, old-MK + new-MK). | — |
| Client-data identity isolation: a future new ONBOARDING route must route its pre-persist wipe through `wipeClientDataForFreshOnboarding` | When a new onboarding/join route is added | **Larissa INFO (2026-07-06).** The onboarding pre-persist wipe now has a single chokepoint (`wipeClientDataForFreshOnboarding`, which skips the wipe when a local account already exists → no returning-user data loss). A new onboarding route that wipes unconditionally would reintroduce the HIGH-1 data-loss class. Also non-blocking: the rare partial-wipe residual (first `Dexie.delete` succeeds, second throws → orphaned knowledge-vectors) is unchanged from `wipeDevice`. |

## Active — Hygiene & Tooling

Small items that don't fit elsewhere.

| Item | Trigger | Notes |
|---|---|---|
| ~~`.envrc` per-subdirectory split (currently single root .envrc collides PORT keys)~~ | Resolved differently — see "Resolved" | — |
| Operator-side admin-client invitation creation UI | Squash C (admin-client) | New scope item added during 2026-05-19 |
| Operator-side invitations list with revoke | Squash C (admin-client) | New scope item added during 2026-05-19 |
| Vite-PWA `dev-dist` already in biome ignore (2026-05-19) | — | Done; example of how a resolved entry looks |
| ~~`full-lifecycle.test.ts` truncates production `auth_db`~~ | Resolved 2026-05-21 — see Resolved | TEST_DATABASE_URL + auth_db_test isolation landed in QA-fixes squash (commit `34f6adb`) |
| `full-lifecycle.test.ts` steps 2-10 broken since `002e6e1` (OPAQUE wire field rename) — step 2 fails with `link/opaque/finish` returning 500 internal | Own follow-up squash after current QA-fixes squash lands | Discovered 2026-05-21 during Task 3 of QA-fixes squash: with the new TEST_DATABASE_URL isolation in place, the test now runs without destroying live data — and reveals it has been silently broken since the wire-field-rename. 1/10 steps pass (bootstrap CLI), rest cascade from step 2. Orthogonal to Task 3's gating concern. The Redis-side test-isolation leak (cross-test rate-limit pollution) is a related but separate issue; flush Redis between full-test runs as workaround. |
| ~~QR-scanner camera stream stays active after navigating away from `/linking/scan`~~ | **STRUCK 2026-07-13 — no longer applicable.** The `/linking/scan` route was deleted by the 2026-05-22 onboarding overhaul; the current scanner (`routes/onboarding/pairing/scan.tsx`) stops the camera on unmount and on success. Original (2026-05-21): observed the device camera staying on after `/linking/scan` → `/linking/confirm` → `/app`; suspected missing qr-scanner `stop()` on unmount. Verified against the current scanner during the 2026-07-13 release-day audit — the cleanup is present. |
| ~~Settings button (and any other session-gated header control) disappears after a service-worker refresh~~ | Resolved 2026-05-21 — see Resolved | `<ProtectedRoute>` wrapper landed for `/app`, `/linking/*`, `/change-passphrase`, `/settings/*` in own squash |
| Bitwarden Android: `NotReadableError` on passkey register due to lack of PRF support | Bitwarden roadmap dependency; revisit when Bitwarden Android ships PRF | Discovered 2026-05-21 during UV-relaxation manual QA: a debug `navigator.credentials.create({ extensions: { prf: ... } })` call to Bitwarden Android consistently threw `NotReadableError: An unknown error occurred while talking to the credential manager.` Bitwarden Desktop is on the ADR-0022 compatibility matrix; Bitwarden Android is not. ADR 0005 (PRF required) correctly refuses the credential. UX-only follow-up: consider mapping `NotReadableError` to a more specific frontend copy ("Your authenticator could not complete the request — try a different one or update your credential manager") in a future copy-polish squash. No security or correctness impact. |
| Settings → "Add biometric on this device" copy now slightly drifts from mechanics (post-UV-relaxation it can register Bitwarden/Yubikey passkeys, not just biometrics) | Phase-1 UX polish, own copy/mechanics-alignment squash | Flagged by Larissa during the UV-relaxation courtesy pass (2026-05-21). `registerLocalBiometric()` in `apps/user-client/src/lib/webauthn.ts` neither sets `authenticatorAttachment: 'platform'` nor `userVerification: 'required'`, so the "Set up biometric on this device" Settings button can in fact register cross-platform passkeys. Lyra's brief explicitly retained the biometric copy on the Settings surface (the flow is most often biometric in practice), but the semantic drift is real. Two fix shapes: (a) make the copy generic ("Set up passkey on this device") — preferred from a "describe what happens" stance; (b) add `authenticatorAttachment: 'platform'` to constrain the flow to actual platform biometrics — preferred from a "make the copy true" stance. Decision deferred to a copy/mechanics-alignment squash post-Phase-0. No security or correctness impact. |
| `prom-client` metrics half of retry observability (`llm_upstream_retries_total{provider,status,operation}` + retry-delay histogram) | Phase-2 proxy-service (first server-side call-site for llm-unified) | The sink-agnostic `onRetry` hook is in place; the proxy attaches its prom-client sink to the same callback. Spec: `superpowers/specs/2026-05-31-retry-observability-design.md` §4.3. |
| ~~`session.mk` disappears after disconnect-without-logout in user-client~~ | Resolved 2026-05-21 — see Resolved | Shape-A store-slice refactor in QA-fixes squash (commit `34f6adb`); manual QA by Chris confirmed disconnect-then-relink works without the logout+login workaround |
| 8 pre-existing user-client test failures (`cockpit-draft` ×6, `chat-page`, `chat-route`) — `localStorage` undefined in those files' jsdom setup | Own small test-infra squash | Long-standing baseline (noted across bookmarks/credential-bus STATUS as "592/8", now "657/8" after message-rendering). `loadLazyDraft` → `localStorage.getItem` throws because those files don't get the `localStorage` global the rest do (likely a missing setup import or per-file environment quirk). Confirmed failing on `master` independent of any feature (verified 2026-06-01 during the message-rendering finish). |
| ~~Mermaid leaves orphan "bomb" error graphics on render error~~ | Resolved 2026-06-01 (in the message-rendering squash `433cd79`) | Manifested immediately on Chris's device test: every partial mermaid fence streamed token-by-token failed to parse, and mermaid's default error rendering injected its uncleaned "bomb" error SVG into the document — many piling up on screen. Fixed by gating `render()` behind `parse({ suppressErrors: true })` (invalid/incomplete sources never reach the renderer) plus `suppressErrorRendering: true`. Was the opus review's confidence-55 note, under-rated at the time. |
| Orphaned-secret decrypt failure surfaces as an uncaught `OperationError`, silently killing the send (no constructive recovery cue) | Own small squash — constructive error handling | Discovered 2026-07-04 during Chris's first backend test. `openSecret(provider.apiKey, mk, …)` in `send-message.ts:150` (`resolvePersonaContext`) throws WebCrypto `OperationError` when the provider key was sealed under a prior MasterKey (the fresh-join-overwrote-MK class — see STATUS-TRANSITION line 107/118). The rejection is uncaught (`secrets.ts:76`), so the message just fails to send with only a console error. Violates the constructive-error-handling tenet (the *dere* half). Principled fix: catch the decrypt failure at the `openSecret` call sites (chat send + regenerate + expert + image paths all pass `mk`) and surface an actionable message — e.g. "This provider's saved key can't be unlocked with your current account — re-enter it in Settings → Providers" — ideally with a direct affordance and the user's input preserved. This is the user-facing safety net for the deferred local→linked foreign-MK adoption gap (couplings register line 46-51) and any future MK divergence. NOT broken keys and NOT a mis-displayed error — a genuine decrypt failure. Recovery for existing orphaned providers: re-enter each key (re-seals under the current MK); MK_A is lost (fresh-join overwrote it, no-recovery). |
| ~~wafer.ai + xAI: entering the key + sending returns `400 bad_target` via the CORS proxy~~ | **RESOLVED** — squash `62874ec4` landed on master 2026-07-04 (regression-tested). **One real xAI or wafer send with a real key through the proxy still owed to Chris (go-live checklist).** | Root cause (same for both, not a wafer-specific issue): both are `corsHint: 'requires-proxy'` AND their `baseUrl` carries a path (`https://api.x.ai/v1`, `https://pass.wafer.ai/v1`). The client transport sent the full baseUrl as `x-cors-proxy-target`, but `apps/proxy-service` `parseTarget` refuses a target with a path (origin-only contract) → 400 `bad_target`. ollama-cloud (also requires-proxy, bare-origin baseUrl) was unaffected. Fix in `packages/llm-unified/src/transport.ts` (cors-proxy branch): compute the full upstream URL as the direct route would, then split — `x-cors-proxy-target` = origin, path+query onto the proxied request line. Also silently fixed proxied web-search (ollama/nano-gpt, baseUrl `/api`), which had the same latent breakage. TDD regression test (path-bearing baseUrl) + web-adapter fixtures corrected; llm-unified 421 green, proxy-service 81 green, typecheck 14/14; Larissa CLEAR TO COMMIT (SSRF/pin/no-userinfo/secret controls all preserved). REMAINING: Chris re-enters xai + wafer keys, rebuilds llm-unified + restarts the client, and confirms a real send end-to-end; then squash. (wafer may still have a separate ZDR-header nuance — cf. [[project_xai_zdr_negotiation_possible]] — but that is downstream of the 400 being gone.) |
| `pnpm dev` starts the backend services WITHOUT `--env-file=.env.dev`, so `OPAQUE_SERVER_SETUP` is unset → ephemeral per-process OPAQUE setup that changes on every restart, permanently invalidating every registered account's passphrase auth | Own small tooling squash | Discovered 2026-07-04 during Chris's first backend test: recurring "Server auth failed" (`server_auth_failed` badge) on sign-in. Traced to two divergent start paths — `./dev.sh` runs `bun --env-file=.env.dev --watch` (loads the pinned setup, correct), but `pnpm dev` → `turbo run dev` → the per-workspace `bun --watch src/index.ts` script auto-loads only `.env` (22 May, no `OPAQUE_SERVER_SETUP`) → the silent ephemeral fallback in `apps/auth-service/src/opaque/server.ts:34`. Symptom: `login/start` returns 200 but the client-side `opaqueLoginFinish` throws `wrong_passphrase` before `login/finish` is ever sent, because the server `login_response` is signed under a setup key that no longer matches the registration record. Workaround in place: start the backend via `./dev.sh`, not `pnpm dev`. Durable fix options (Chris chose immediate-only 2026-07-04): (a) align the per-workspace `dev` scripts to `bun --env-file=.env.dev --watch src/index.ts` so `pnpm dev` == `./dev.sh`; (b) make `getServerSetup()` throw outside `NODE_ENV=test` when unset, converting the silent footgun into a loud boot failure (also protects self-hosters). No code change committed this session. |
| Crypto: `join-by-pairing.ts` `isEvidenceInvalidError` (line ~327) checks the dead string `opaque_evidence_invalid`; the server emits `opaque_authentication_failed`, so the branch never matches and the raw `HttpError` propagates | Opportunistic cleanup | **Surfaced by the opus whole-branch review of the pre-release blocker fixes (2026-07-05).** Blocker-2's `mapError` already handles the rethrown raw error inline, so behaviour is correct — but the crypto helper + its unreachable `CryptoError('opaque_protocol_error')` conversion are latent cruft. Deliberately left untouched by blocker 2 (touching it would route the rare server-desync case into a worse fatal screen + make the commit a Larissa path). Drop or realign next time the file is touched. |
| ~~`recovery.test.ts` `buildProof` uses the wrong serverId (`…/auth/v1` vs the actual origin `/v1`) → that integration test is red in every environment~~ | **RESOLVED 2026-07-06** (pre-test-analysis fixes) | The same stale `${API_BASE_URL}/v1` OPAQUE server identity turned out to sit in **13** integration-test files — it was the root cause of the ENTIRE long-carried auth-service "OPAQUE baseline" failure set, not just `recovery.test.ts`. All fixtures aligned with `opaqueServerIdentity(origin)`; plus the bootstrap CLI tests' hard-coded `/home/chris/…` cwd made portable and the spawned CLI given `env: {...process.env}`. Full auth-service suite green for the first time: **174 pass / 12 skip / 0 fail**. |

---

## Resolved

When an entry above is fully resolved, move it down here with a brief
note. This is a deliberate audit trail — "what we deferred and how it
landed" — not garbage to be cleaned.

| Item | Resolved how | Date |
|---|---|---|
| ~~Vite-PWA `dev-dist` files lint-noisy in biome~~ | Added `dev-dist` to `biome.json` ignore list during Squash D pre-squash cleanup | 2026-05-19 |
| ~~Add-biometric button hardcoded `disabled` in Settings → Auth methods~~ | Wired up to `registerLocalBiometric` during Squash D follow-up | 2026-05-19 |
| ~~PRF salt mismatch between registration and unlock~~ | Both sites now use `PRF_INPUT_SALT`; fixed in Squash D follow-up | 2026-05-19 |
| ~~Wrong public-key format stored (SPKI instead of COSE)~~ | Extract COSE from authenticatorData in `webauthn.ts`; fixed in Squash D follow-up | 2026-05-19 |
| ~~Missing Sign-out button in Settings → Account~~ | Added in Squash D follow-up | 2026-05-19 |
| ~~Horizontal overflow on narrow viewports from BreathingOrb absolute positioning~~ | Added `overflow-hidden` to section + `overflow-x-hidden` to root layout in Squash D follow-up | 2026-05-19 |
| ~~Regenerate-recovery-key button silent disabled on biometric session~~ | Inline hint added below button explaining the limitation | 2026-05-19 |
| ~~Decision: passkey UV-relaxation Q1–Q4~~ | All four decided; awaiting Lyra formal brief + ADR | 2026-05-19 |
| ~~Decision: cross-device identity Q1–Q6 plus merge strategy~~ | All decided; awaiting Lyra formal brief | 2026-05-19 |
| ~~`.envrc` global PORT/DATABASE_URL collision (proxy-service overrode auth-service)~~ | Removed app-level `dotenv_if_exists` from root `.envrc`. Each runtime (Vite for frontends, Bun for backends) auto-loads its own `apps/<app>/.env` from cwd. Subdirectory `.envrc` files unnecessary. Discovered 2026-05-20 during Squash C QA when DATABASE_URL pointed to `proxy_db` for auth-service bootstrap. | 2026-05-20 |
| ~~Passkey UV-policy: formalise brief + write ADR 0022~~ | Brief `obsidian/briefs/phase 0/passkey-uv-policy.md` + [[ADR 0022]] landed. Sibling insights [[2026-05-20-pattern-frontend-changes-affecting-crypto-semantics]] and [[2026-05-20-pattern-passive-auth-state-on-profile]] captured related design principles for later. | 2026-05-20 |
| ~~Cross-device identity: formalise full brief~~ | Brief `obsidian/briefs/phase 0/cross-device-identity.md` landed with all seven Q1–Q6 sub-decisions plus the emergent merge-strategy decision. One [OPEN] remains (#3, API curl-verification — Chris-tracked); item moved to Active — Design. | 2026-05-20 |
| ~~ADR — server-at-domain-root, HTTPS, `/api` prefix~~ | [[ADR 0023]] landed. | 2026-05-20 |
| ~~ADR — single-server-per-account hard rule~~ | [[ADR 0024]] landed. | 2026-05-20 |
| ~~ADR — UUIDv7 across the entire data model~~ | [[ADR 0025]] landed (was implicit in originating material, elevated to its own ADR during brief formalisation). Library choice: `uuidv7` npm package. | 2026-05-20 |
| ~~Pre-disconnect-sync-pull state-machine ADR~~ | [[ADR 0026]] landed, with re-ordered step sequence that eliminates the no-active-server transient by deferring the Y-logout to the final step. | 2026-05-20 |
| ~~Rate-limiting numbers for pairing-code generation~~ | Decided in cross-device-identity brief §Rate limits: 10 active codes/user, 50 generations/24h, 10 join-attempts/min/IP, 100/h/IP. | 2026-05-20 |
| ~~Step-up authentication for sensitive operations: formalise brief + ADR~~ | Brief `obsidian/briefs/phase 0/step-up-auth.md` + [[ADR 0027]] landed. Tiers 0–4, mechanisms A (UV='required' WebAuthn) / B (OPAQUE re-prompt) / C (grace window), Redis-backed state, single `POST /v1/auth/step-up` endpoint. Inline minimums in cross-device-identity brief remain authoritative for that brief's standalone implementation and are equivalent to formal tiers. | 2026-05-20 |
| ~~Provider/model handling rework: spec → implementation~~ | Implemented per spec/plan and squashed to `4c54661` (derived `ServiceKind` caps, global CORS-proxy home, configured-only add-picker + model picker, all 6 deredere extras). Awaiting Chris's device test (spec §12) before push. Insight: [[2026-06-01-provider-model-rework-sweetspot]]. | 2026-06-01 |
| ~~`full-lifecycle.test.ts` truncates production `auth_db`~~ | Resolved via TEST_DATABASE_URL + dedicated `auth_db_test` Postgres database (auto-created by `infra/postgres/init/02-create-test-db.sql` on first compose-up; ad-hoc on running instances). The test refuses to run without `TEST_DATABASE_URL` and throws if its normalised host+pathname matches `DATABASE_URL`. QA-fixes squash (commit `34f6adb`). | 2026-05-21 |
| ~~`session.mk` disappears after disconnect-without-logout~~ | Resolved via Shape-A session-store refactor: `mk` is now a separate store slice (not a property on the spread-able session), `setSession(session, mk?)` preserves the existing `mk` when the second arg is omitted. Partial-spread drops are structurally impossible. Manual QA confirmed disconnect-then-relink works without the logout+login workaround. Larissa-approved-with-defer (Critical: none, High: none; L-3/L-4 deferred). QA-fixes squash (commit `34f6adb`). | 2026-05-21 |
| ~~Wire UV-relaxation in code (3 sites)~~ | Resolved via UV-relaxation-wiring squash (commit `e814e87`). All three ceremony sites in apps/user-client now use UV='preferred' per ADR 0022; PRF (ADR 0005) untouched. Login gate rewidened from UVPAA-only to any WebAuthn-capable device. Unlock copy renamed biometric→passkey. Larissa-approved (no findings). | 2026-05-21 |
| ~~Settings button disappears after service-worker refresh~~ | Resolved via `<ProtectedRoute>` wrapper on `/app`, `/linking/*`, `/change-passphrase`, `/settings/*`. When in-memory session is null after reload, the wrapper redirects to `/` so `Gate` can decide the right destination (`/login` or `/onboarding`). Pre-existing routing bug, surfaced during UV-relaxation manual QA. | 2026-05-21 |
| Deployment kit — backend image ships `devDependencies` | Runtime stage copies the whole builder `/app` (no `--prod` prune), enlarging the image / attack surface. Tune image size in a later pass (pnpm prod prune in a monorepo is fiddly). Non-blocking. | 2026-07-07 |
| Deployment kit — root `README.md` dev proxy-port drift | Root README still lists the dev proxy port as `3300` in 3 untouched lines while `apps/proxy-service/.env.dev` uses `8090`/OPS `3300`; DEPLOYMENT.md §4.3 was correctly reconciled to prod `8080`/`9090`. Pre-existing dev-doc drift, out of the kit's scope. | 2026-07-07 |
| Deployment kit — `install.sh` re-run mints a 2nd bootstrap invitation | `bootstrap-admin` only refuses once `auth_methods` is non-empty, so re-running `install.sh` before the first admin registers mints a second 24 h `primary_admin` invitation. Harmless (both valid, one expires); upstream CLI semantics. Accept or tighten later. | 2026-07-07 |
| ~~Deployment kit — `TRUST_PROXY_HOPS` on auth (go-live blocker)~~ | **Resolved 2026-07-10** — `TRUST_PROXY_HOPS` added to auth (`env.ts`, default 1), the boolean `RATE_LIMIT_TRUST_FORWARDED_IP` replaced, socket peer injected in `index.ts`, and the deploy kit wires it through (`compose.template.yml`, `deployment.env.template` already had it). Larissa CLEAR. | 2026-07-07 → 2026-07-10 |
| ~~Sync: `setAttention` clobbers the sticky `tamper` integrity alarm (MEDIUM, 2026-07-13 audit)~~ | **Resolved 2026-07-13** (Unit B, squash `b4a4944b`). A guard in `setAttention` makes a persisted `tamper` attention un-overwritable by routine notices (tombstone waves, quota/rate-limit); legitimate wholesale resets bypass `setAttention` entirely. Larissa courtesy CLEAR (monotone, strengthens the alarm; also fixes a latent same-loop clobber in `recoverBlobs`). | 2026-07-13 |
| ~~Sync MEDIUM-1: pushed `vectors` ciphertext never tombstoned server-side on document/library delete~~ | **Resolved 2026-07-13** (Unit C, squash `6b143299`). Document/library delete cascades now enqueue a `vectors` tombstone per synced vector row (same blind-id key path as the upsert — verified byte-for-byte), and pulled vector tombstones are excluded from the user-facing `tombstone_threshold` tally. Larissa courtesy CLEAR. | 2026-07-13 |
| ~~Sync MEDIUM-3: mass-delete blind-id CPU (stage-1 re-derives per tombstone, N×M)~~ | **Resolved 2026-07-13** (Unit B, squash `b4a4944b`). A per-cycle stage-1 blind-id memo (same MK-scoped lifecycle as the existing stage-2 cache) drops derivations from N×M to ≤M+N. Bundled with MEDIUM-1, which multiplies tombstone volume. | 2026-07-13 |
| ~~Recovery: onboarding `not_found` / `invalid_recovery_key_format` fell through to a generic error~~ | **Resolved 2026-07-13** (Unit D, squash `cf5b3f77`). Unknown username → a named fatal screen; malformed key → an inline non-fatal field error (copy reused verbatim from the login surface), input preserved; honest 429 copy from `Retry-After`. Laura clean. | 2026-07-13 |
| ~~Recovery flow-R: a wrong key on the deferred passphrase screen was a reload-only dead-end~~ | **Resolved 2026-07-13** (Unit D, squash `cf5b3f77`). A "Re-enter recovery key" back affordance returns to the key step (field cleared + focused, scope preserved); `mapOnlineRecoveryError` now distinguishes 429 / 404 from genuine unreachable. `step2-local` correctly excluded (a wrong key cannot occur there). Laura clean. | 2026-07-13 |
| ~~Proxy M1: requires-proxy send with no linked account gave a generic "couldn't reach the model" with the remedy two screens away~~ | **Resolved 2026-07-13** (Unit E, squash `cd0a3935`). A typed `ProxyUnavailableError` (llm-unified) now drives a constructive toast + a "Open Server linking" footer button → `/app/account/server-linking`; background jobs stay silent. Laura clean. | 2026-07-13 |
| ~~Orphaned relay secret — sealed `sharedKey` under `settings.corsProxy` kept forever after the relay cut `94bdcdd6`~~ | **Resolved 2026-07-13** (Unit F, squash `16890861`). A load-time settings normaliser clears the dead sealed blob to the fresh-install `null` shape and persists it; no Dexie bump (non-indexed). | 2026-07-13 |
| **Local-only → server account upgrade path does not exist** | A design question for Lyra/Chris. Surfaced by the 2026-07-13 `/join` work (spec §2.5(3)): a local-only session-holder who scans an invitation is redirected to `/app` with a toast that *names* the limitation ("joining a server from a local account isn't available yet") rather than dead-ending. There is currently no in-app flow to uplevel a local-only account onto a server. Candidate work: an explicit local→server link/merge flow. | 2026-07-13 |
| Pre-go-live fix-bundle deferred minors (non-blocking, opportunistic) | From the 2026-07-13 bundle's reviews, all defer-approved: (a) `rateLimitMessage` always plural → "about 1 minutes" for a sub-90s `Retry-After` (unlikely given the 10-attempts/15-min window); (b) the "No account…" literal is duplicated in the live arm + a commented dead HttpError backstop in onboarding recovery; (c) the `/join` invitation-tile prefill is asserted against a stubbed form (the pairing tile proves the identical mechanism against the real form); (d) `ClientDataDb` was `export`ed to type an F1 helper param — a narrower `Table<SettingsRow,1>` param would avoid widening the singleton module surface (`settings.ts` imports it type-only, so safe today). | 2026-07-13 |
| ~~Recovery: unknown-username / rate-limit shown as a fatal wall in onboarding (inline in login)~~ + ~~relay-cut Retry gives no hint it will fail until linked~~ | **RESOLVED 2026-07-13** (Chris-approved Laura softs; squashes `1da337f2` + `b5b1ee21`). Onboarding recovery now surfaces unknown-username (inline username-field error, copy a live reference to the login surface's `unknownUsername`) and the 429 rate-limit as INLINE non-fatal errors, inputs preserved — fix-in-place in a stressful moment; genuinely-terminal fatals (`conflict`, ≥500/network) stay fatal. The relay-cut footer now shows a calm "Retry will work once your account is linked." hint without disabling Retry (works-after-link preserved), gated `!retryDisabled` so the two reason slots stay mutually exclusive. Code review + Laura confirm: no defects. | 2026-07-13 |
| ~~Recovery/footer residual softs (conflict "Try again" + rate-limit wording)~~ | **RESOLVED 2026-07-13** (Chris-approved; squashes `37d51609` + `4f84cd93`). (a) The `conflict` fatal's action now reads "Back to onboarding" and routes to `/onboarding` (a `Screen.action` discriminant; the retryable fatals keep "Try again"). (b) `rateLimitMessage` extracted to one shared `lib/recovery-copy.ts` used by both recovery surfaces — login now shows the same honest `Retry-After`-derived wait-time; the plural glitch fixed (exactly 1 → "about 1 minute"); the orphaned static `rateLimited` copy removed. Code review Approved. **Residual nit (opportunistic):** the `Screen.action` type is `'retry' \| 'onboarding'` but `'retry'` is never constructed (non-conflict fatals omit it) — could narrow to `action?: 'onboarding'`; behaves correctly. | 2026-07-13 |
| `run-ollama-suite.ts` iterates the web offerings too | The loop runs every `ollamaCloud.offerings`, including `web-ollama-search` / `web-ollama-fetch`, building an LLM adapter for a search slug — both report FAIL (HTTP 404) on every scenario in a live run. Noise, not signal, and it makes a green run harder to read. The file's header comment is stale in the same breath: it claims ollama uses the generic path via `makeGenericLiveBinding`, while the code uses `makeLiveBinding` + the native adapter. Related: `binding.ts:106` still documents `makeGenericLiveBinding` as serving "vanilla OpenAI-compatible providers (e.g. ollama-cloud)" — wrong since 2026-06-03. Pre-existing; deliberately out of scope for the 2026-07-17 background-job fix. | 2026-07-17 |
| ~~GLM 5.2's `fixed-on` reasoning classification is probably wrong~~ | **RETRACTED 2026-07-17, same day — the claim was mine and it was wrong.** `fixed-on` for GLM 5.2 is **correct**: `think:false` empties the thinking channel but does not stop the model reasoning — it relocates it into the answer (content 869 → 3265 chars, eval_count 526 → **1010**). "Off" costs ~2x and only lengthens the reply, which is exactly the `fixed-on` / "off only hides" case. The original error was a **bad experiment**: it probed with a title prompt that never triggers reasoning, so a short clean answer "proved" disabling. Re-probed with a reasoning-warranting prompt, the opposite holds. Lesson worth more than the finding: a negative result only counts if the stimulus was present. | 2026-07-17 |
| ~~`glm-5.1` / `deepseek-v4-pro` `fixed-on` is probably wrong~~ (GLM 5.2 is NOT) | **Resolved 2026-07-17** (Chris approved on the evidence). Both are now `{ mode: 'toggle', defaultOn: true }`; GLM 5.2 stays `fixed-on`. Broad probe (n=5 × 2 reasoning-warranting prompts): glm-5.1 eval_count −65%/−72%, deepseek −50%/−60%, content unchanged, no truncation — a genuine off-switch, contradicting the 2026-06-03 "no-op on these models" line. GLM 5.2 was the **control** and behaved differently (content 3-4x longer), which is what kept it `fixed-on`. **Method note worth keeping:** the discriminator is *content length*, not `eval_count` — GLM 5.2's eval_count also fell on one prompt (−26%), which alone would have read as an off-switch and produced a third wrong classification. `defaultOn: true` because reasoning is the point of a reasoning-native model and quality degrades without it. The live suite now verifies it itself (toggle → two permutations; `reasoning-absent` + `reasoning-present` both PASS on each). | 2026-07-17 |
| `/v1` is a measured-viable fallback; the native adapter's stated reason is gone | The 2026-06-03 finding that justifies `ollama-native.ts` (the `/v1` shim re-calls the tool instead of answering after a tool result) **does not reproduce**: 18/18 answered across glm-5.1, glm-5.2:cloud and deepseek-v4-pro with the real `buildPrompt` prompt, streaming, 3 runs per cell (2026-07-17). Native is kept for reasons that stand alone (first-class API, atomic tool-call args, and it is the *smallest* adapter in the repo at 160 lines — a `/v1` adapter would be a 7th clone of the OpenAI SSE parse, net +15..30 lines). Revisit deliberately if a shared `openAiCompatAdapter` for the six near-duplicate OpenAI adapters is ever built; that would make `/v1` nearly free. All measurements are in the 2026-07-17 spec, so no re-probing is needed. | 2026-07-17 |
| Ollama `top_k` / `min_p` deliberately unmapped in `mapSampling` | Contested in review and ruled by Chris: both are in ollama's documented `options` schema, and `bodyExtras` is a generic `Record`, so a future caller adding ollama-specific tuning would have them **silently dropped** — the exact failure class the 2026-07-17 fix exists to remove. Ruled out on YAGNI: neither is an OpenAI-side param, nothing sends them, no UI exposes them. Two lines and a test whenever someone needs them. Note the mapping rule is "the params we send", not "the params ollama accepts" — the `ollama-native.test.ts` case named "drops keys ollama does not accept" overstates its premise. | 2026-07-17 |
| A shared `openAiCompatAdapter` for the six near-duplicate OpenAI adapters | chutes 175, wafer 190, tensorix 187, mistral 238, openrouter 264, xai 267 lines — each reimplementing the same OpenAI SSE parse with tool-call fragment accumulation (1 321 lines between them). This duplication is the real reason moving ollama to `/v1` would *add* code rather than remove it, and it is the lever that would change that calculus. Own spec; not a bugfix. | 2026-07-17 |
| Mild message editing — deferred minors | Non-blocking, from the whole-branch (opus) + Laura pre-squash reviews of `b60f78c1`: (a) per-item undo of a staged attachment removal has no UI — a removed original is restored only via full Cancel (obvious hook: `unstageRemoval`, already present + tested); (b) `enterEdit` overwrites a pre-existing half-typed new-message draft / pending attachment (rare same-session edge); (c) staged attachment *additions* survive a reload (real pending rows) while *removals* don't — reconcile spec §8 wording; (d) prior-message attachments are not copied on edit→Branch (matches `useBranchChat`); (e) no end-to-end test on `chat-page.onSend` routing (the pure `resolveSendAction` decision IS tested; the wiring rests on review + manual verify); (f) `message-edit.test.ts` lacks createdAt-tie / empty-list cases (behaviour sound). | 2026-07-18 |
